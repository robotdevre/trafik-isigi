name: Wokwi Test

on:
  push:
    paths:
      - '**.ino'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Show code
        run: cat trafik-isigi.ino
